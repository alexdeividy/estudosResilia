SELECT * FROM TB_CLIENTE OFFSET 2 LIMIT 5

SELECT * FROM TB_PEDIDO
LEFT JOIN TB_CLIENTE
ON TB_PEDIDO.ID_CLIENTE = TB_CLIENTE.ID_CLIENTE;

INSERT INTO tb_cliente (nome,email,endereco) VALUES
	 ('Marcelo Lopes','m.lopes@fb.com','av. Rocha, numero 2, Rio Vermelho'),
	 ('Helena Alencar','hele22@fb.com','av. das Orquídeas, numero 4, Liberdade'),
	 ('Almeida Santos','santo.almeida@xpto.com','rua das Flores, quadra C, numero 14, Liberdade');
	 
SELECT A.NOME, D.NOME FROM TB_CLIENTE A -- FORMA ERRADA USANDO RIGHT JOIN --
RIGHT JOIN TB_PEDIDO B ON B.ID_CLIENTE = A.ID_CLIENTE
RIGHT JOIN TB_PEDIDO_PIZZA C ON C.ID_PEDIDO = B.ID_PEDIDO
RIGHT JOIN TB_PIZZA D ON D.ID_PIZZA = C.ID_PIZZA;

SELECT D.NOME, A.NOME FROM TB_PIZZA A -- FORMA CORRETA USANDO RIGHT JOIN --
RIGHT JOIN TB_PEDIDO_PIZZA B ON B.ID_PIZZA = A.ID_PIZZA
RIGHT JOIN TB_PEDIDO C ON C.ID_PEDIDO = B.ID_PEDIDO
RIGHT JOIN TB_CLIENTE D ON D.ID_CLIENTE = C.ID_CLIENTE
ORDER BY A.NOME DESC;

-- ATIVIDADES AULA 12.1 --

SELECT A.NOME, B.ID_PEDIDO FROM TB_CLIENTE A
RIGHT JOIN TB_PEDIDO B ON B.ID_CLIENTE = A.ID_CLIENTE
ORDER BY A.ID_CLIENTE;

-- ATIVIDADES AULA 12.2

